# Optional helpers under python/ — not required for the main DXA Agent CLI.
# Install:  pip install -r python/requirements.txt
#
# Python 3.10+ recommended (3.11+ preferred).

httpx>=0.27.0,<1
pytest>=8.0.0,<9
pytest-asyncio>=0.24.0,<1
